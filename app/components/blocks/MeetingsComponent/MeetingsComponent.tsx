import React, { FC } from 'react'
import { View, Text } from 'react-native'

const MeetingsComponent: FC = () => {
	return (
		<View>
			<Text>MeetingsComponent</Text>
		</View>
	)
}

export default MeetingsComponent
